<div class="row">
  <aside class="col-md-4">
    <%= render 'form' %>
  </aside>
    <table class="my-receivers">
        <thead>
          <tr>
          <td>Receiver</td>
          <td>Phone Number</td>
          </tr>
        </thead>

        <tbody>
        <% @receivers.each do |receiver| %>
          <tr>
          <td><%= receiver.name %></td>
          <td><%= receiver.phonenumber %></td>
        </tr>
        <% end %>
      </tbody>
    </table>
</div>
